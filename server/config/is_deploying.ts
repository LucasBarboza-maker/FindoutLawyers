/*
:--------------------------------------------------------------------------
: Requirements
:--------------------------------------------------------------------------
*/

import { ENV } from './env'

/*
:--------------------------------------------------------------------------
: Overall use toward server
:--------------------------------------------------------------------------
*/

export function isDeploying(): boolean {
   return !!ENV.PRODUCTION
      ? eval(ENV.PRODUCTION)
      : false;;
}
